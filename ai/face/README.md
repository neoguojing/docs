    • 园区安防：围绕人员、车辆等目标进行识别、告警、布控和轨迹分析。
    • 人员能力：人脸、人像/人体分析、人脸人体聚类，形成从“单次抓拍”到“人员档案”的能力。
    • 算法工程化：支持检测、分类、实例分割、语义分割等视觉算法类型。
    • 训练推理一体：训练侧产生模型后，由推理平台负责模型进入运行环境、算法启动、部署和持续推理。
    • 数据闭环：算法输出不仅是一个类别或框，还包括抓拍图片、结构化数据、特征，并进入后续聚类/大数据分析。

    
```
                         业务层
                            │
                ┌───────────┴───────────┐
                │                       │
             视频任务                  图片任务
                │                       │
                ▼                       ▼
       视频流处理引擎管理模块       图片处理引擎
                │                       │
        资源评估 + 任务调度             │
                │                       │
                ▼                       ▼
       视频处理引擎实例              图片算法Pipeline
                │                       │
       ┌────────┴────────┐        ┌─────┴──────┐
       │                 │        │            │
    VIS拉流           节点资源   目标检测    特征提取
       │                          │            │
       ▼                          └─────┬──────┘
     解码                               │
       │                                ▼
       ▼                         静态特征库检索
   Frame                              │
       │                                ▼
       ▼                         相似度/布控比对
  算法 Pipeline                         │
       │                                ▼
       ▼                              结果
   结果处理
       │
   ┌───┴────────┐
   ▼            ▼
时空库         MinIO
```

```
                    业务应用层
┌──────────────────────────────────────────────┐
│ 安防监控 / OCR / 人脸识别 / 工业检测 / 视频分析 │
└───────────────────────┬──────────────────────┘
                        ↓
                    Pipeline层
┌──────────────────────────────────────────────┐
│ 检测 → 跟踪 → 特征 → 属性 → 识别 → 结果融合    │
└───────────────────────┬──────────────────────┘
                        ↓
              ★ Kestrel 推理运行时 ★
┌──────────────────────────────────────────────┐
│ 算法插件 / 模型加载 / Pipeline / 数据管理       │
│ 输入输出抽象 / 设备抽象 / 内存管理 / 并发       │
└───────────────────────┬──────────────────────┘
                        ↓
              推理框架 / Backend层
┌──────────────────────────────────────────────┐
│ TensorRT / CUDA / ONNX Runtime / 国产推理SDK   │
└───────────────────────┬──────────────────────┘
                        ↓
                    硬件层
┌──────────────────────────────────────────────┐
│ NVIDIA GPU / 国产GPU / TPU / NPU / CPU        │
└──────────────────────────────────────────────┘
```
