

## 数据库
事务（Transaction）：在数据库语境中，指构成一个逻辑单元的一组读写操作
索引: 可以极大地加快读取速度，但通常会拖慢写入速度
      R-Tree  多列联合索引 全文检索 
存储引擎:  日志结构： 只允许向文件追加数据（顺序写）和删除过时文件，从不修改已经写出的文件。
          Memtable SSTable WAL Bloom Filter Compaction
          就地更新： B树 把磁盘划分为固定大小的页（Page/Block，通常为 4KB~16KB），并允许在原位置直接覆写数据。
          WAL 
 行式存储 vs 
 列式存储：列压缩
 
事务型系统 MySQL PostgreSQL MongoDB
  原子性 一致性 隔离性 持久性
  读已提交 (Read Committed) 
  PostgreSQL 的“可重复读: 快照隔离 与 MVCC 
  MySQL 的“可重复读: 致性弱于快照隔离的 MVCC 实
  可串行化 (Serializa) : 在单线程上严格按顺序执行事  两阶段锁定(索引范围锁) 可串行化快照隔离

分布式事务： 跨越多个节点的事务

分析型系统 clickhouse
- 两阶段提交
- 
关系模型 vs 文档模型(1:N)
规范化 vs 反规范化 ： 是存储 ID 还是存储文本字符串

ORM: 解决阻抗不匹配

## 缓存 衍生数据系统 

## 搜索索引 衍生数据系统


## 流处理
CQRS（命令查询责任分离）： 从只追加的事件日志（写入优化）中，异步派生出独立的视图（读取优化）。这源于领域驱动设计（DDD）思想，本质是维护两个或多个不同的数据模型以适应读写的分离。

## 批处理

## 分布式系统：
单一庞大的系统拆分为由多台机器通过网络通信协作的系统
无共享架构（Shared-nothing / 向外扩展 Scale-out）

数据分布的基本方式
复制（Replication）： 在不同节点上保留相同数据的副本，用于冗余容错、提升吞吐量与降低访问延迟。
分区（Partitioning / Sharding）： 将大型数据库拆分为较小的子集（分片），指派给不同的节点处理。
  基本定义：通常情况下，每条数据（记录、行或文档）属于且仅属于一个分片。
  分片与复制的结合：分片通常与复制（Replication）结合使用，每个分片的副本存储在多个节点上以获得容错能力。在单主复制模型中，每个分片有一个领导者（Leader）和多个追随者（Follower）。一个节点可以同时是某些分片的领导者和其他分片的追随者。

单主复制
PostgreSQL 的 LSN，MySQL 的 binlog 
脑裂（Split-Brain）： 旧主库因网络抖动“假死”后恢复，系统出现两个主库同时接收写入。通常使用栅栏（Fencing）机制强制关闭或隔离旧主库。
无主复制：Cassandra 法定人数（Quorum）原则

复制延迟与一致性模型: 写后读一致性  单调读 一致前缀读
 复制日志（Replication Log）: 基于语句 WAL 逻辑/行日志 
###  系统设计
系统设计的首要逻辑是：存储和计算数据的价值，必须高于其成本。

向后兼容（Backward Compatibility）： 较新的代码可以无缝读取由较旧代码写入的数据。
向前兼容（Forward Compatibility）： 较旧的代码可以无缝读取由较新代码写入的数据。

#### 非功能性需求的“三大支柱”
可靠性（Reliability）： 系统在面临硬件故障、软件错误或人为失误时，仍能继续正确运行。
可伸缩性（Scalability）： 当系统面临更大的负载（数据量、流量、复杂性）时，增加计算或存储资源能够显著、成比例地提高系统的处理能力。
可维护性（Maintainability）： 软件生命周期的大部分成本在于维护。
  可运维性： 良好的可观测性（监控、日志、链路追踪），便于团队保持系统平稳运行。
  简单性： 采用一致的抽象和设计模式，避免偶发复杂度，降低新人的认知负荷。
  可演化性： 系统易于修改，能够敏捷地适应未知的业务需求变化

#### 延迟定义：响应时间（Response Time）： = 服务时间 + 排队延迟 + 网络延迟
#### 指标衡量与尾部延迟

#### 高并发系统
- 重试风暴：
- 读放大：（物化视图）
